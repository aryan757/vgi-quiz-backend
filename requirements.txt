# --- API layer ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# --- Config / validation ---
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.1

# --- MongoDB ---
motor>=3.6.0          # async driver (live request path)
pymongo>=4.9.0        # sync driver (seeding script)

# --- LLM orchestration ---
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-anthropic>=0.2.0
openai>=1.50.0
anthropic>=0.39.0

# --- Numerics (cosine similarity for topic matching / dedup) ---
numpy>=2.0.0

# --- Testing ---
pytest>=8.3.0
pytest-asyncio>=0.24.0
mongomock-motor>=0.0.34
