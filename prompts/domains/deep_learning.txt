You are generating MCQ questions for a job-readiness quiz platform for AI/ML engineers.

Domain: deep_learning
Total questions needed: 300

Split exactly as:
- 75 questions with difficulty: "beginner"
- 75 questions with difficulty: "intermediate"
- 75 questions with difficulty: "advanced"
- 75 questions with difficulty: "mixed"

Difficulty calibration:
- beginner   → definitions, basic concepts, what-is questions, fundamental usage
- intermediate → practical patterns, tradeoffs, how-it-works, common mistakes
- advanced   → deep internals, optimization, system design, edge cases, production concerns
- mixed      → scenario-based or comparative questions that combine concepts across levels
               (e.g. "When would you choose LSTM over a Transformer and why?")

Scope — questions MUST collectively cover ALL of the following areas
(spread questions evenly across these areas, do not focus on just one):
  • Neural network fundamentals: forward pass, weights/biases, loss computation,
    backpropagation intuition (conceptual — no calculus derivations), gradient descent
  • Optimizers: SGD, momentum, Adam, RMSprop — when to use which, learning rate effects
  • Weight initialization: Xavier, He initialization — why it matters
  • Activation functions in deep nets: ReLU variants, dying ReLU problem, GELU
  • Training techniques: batch size tradeoffs, learning rate schedules, early stopping,
    gradient clipping
  • RNN: sequence modeling, hidden state, vanishing gradient problem
  • LSTM: forget/input/output gates, cell state, why it solves RNN's long-term dependency problem
  • GRU: how it compares to LSTM, when to prefer it
  • Transformer architecture: self-attention mechanism, query/key/value, multi-head attention,
    positional encoding, encoder vs decoder, why it replaced RNNs
  • Attention patterns: scaled dot-product attention, attention scores interpretation
  • Practical deep learning: overfitting in deep nets, dropout in deep nets,
    batch norm vs layer norm, mixed precision training

Rules (follow strictly):
1. Exactly 300 questions total (75 per difficulty)
2. Every question has exactly 4 options — keys A, B, C, D — positions 1, 2, 3, 4
3. correct_answer is a list with exactly ONE key e.g. ["B"]
4. No LaTeX, no math derivations — real job interview level only
5. Return ONLY the JSON object — no markdown fences, no extra text

Return this exact JSON structure:
{
  "domain": "deep_learning",
  "questions": [
    {
      "difficulty": "beginner",
      "question": "What problem does an LSTM solve that a basic RNN struggles with?",
      "options": [
        {"key": "A", "value": "Processing images faster than CNNs", "position": 1},
        {"key": "B", "value": "Retaining long-term dependencies without the vanishing gradient problem", "position": 2},
        {"key": "C", "value": "Reducing the number of trainable parameters", "position": 3},
        {"key": "D", "value": "Enabling parallel processing of sequences", "position": 4}
      ],
      "correct_answer": ["B"],
      "explanation": "LSTMs use gating mechanisms (forget, input, output gates) to control what information is retained across long sequences, addressing the vanishing gradient issue in standard RNNs.",
      "job_relevance": "Core deep learning interview question for any NLP or time-series engineering role."
    }
  ]
}

Save output as: prompts/domains/deep_learning.json
Then run: python scripts/import_questions.py prompts/domains/deep_learning.json
